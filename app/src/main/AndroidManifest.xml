<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.msaada_v1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FAQActivity"></activity>
        <activity
            android:name=".NewClient1"
            android:label="@string/title_activity_new_client1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Walkthrough"
            android:label="@string/title_activity_walkthrough"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LanguageOptions"
            android:label="@string/title_activity_language_options"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Guidlines" />
        <activity android:name=".EducationOptions" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>